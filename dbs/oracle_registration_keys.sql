-- CLUB.REGISTRATION_KEYS definition

-- DDL generated by DBeaver
-- WARNING: It may differ from actual native database DDL

-- Drop table

-- DROP TABLE CLUB.REGISTRATION_KEYS;

CREATE TABLE CLUB.REGISTRATION_KEYS (
	KEY_ID NUMBER(14,0) NOT NULL,
	REG_ID NUMBER(14,0) NOT NULL,
	KEY_CODE VARCHAR2(50) NULL,
	KEY_NUMBER VARCHAR2(10) NULL,
	CONSTRAINT REG_KEYS_PK PRIMARY KEY (KEY_ID),
	CONSTRAINT SYS_C0011407 CHECK ("KEY_ID" IS NOT NULL),
	CONSTRAINT SYS_C0011408 CHECK ("REG_ID" IS NOT NULL)
);
CREATE INDEX "IDX$$_37920003" ON CLUB.REGISTRATION_KEYS (REG_ID);
CREATE INDEX REGISTRATION_KEYS_CODE ON CLUB.REGISTRATION_KEYS (KEY_CODE);
CREATE INDEX REGISTRATION_KEYS_NUMBER ON CLUB.REGISTRATION_KEYS (KEY_NUMBER);
CREATE UNIQUE INDEX REG_KEYS_PK ON CLUB.REGISTRATION_KEYS (KEY_ID);


-- CLUB.REGISTRATION_KEYS foreign keys

ALTER TABLE CLUB.REGISTRATION_KEYS ADD CONSTRAINT REG_KEYS_REGISTRATION_FK1 FOREIGN KEY (REG_ID) REFERENCES CLUB.REGISTRATION(REG_ID) ON DELETE CASCADE;